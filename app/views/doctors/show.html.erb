<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2>
<h3>Patients</h3>
<ul>
    <% @doctor.appointments.each do |a| %>
        <li><%= link_to a.patient.name, patient_path(a.patient) %><br>
        <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
    <% end %>
</ul>